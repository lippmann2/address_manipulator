<%# provide(:some_html, 'This space intentionally left blank.') %>

<!DOCTYPE html>
<html>
  <head>
    <title>Address Manipulator Demo</title>
  </head>
  <body>

    <h1>Address Manipulator Demo</h1>

	<p>Find me in app/views/static_pages/home.html.erb</p>

	<p>This is a simple smoke test by Mark Lippmann to load some remote JSON data from an API and serve it on Rails.</p>

	<p>The code is available in Mark's public GitHub respository.</p>

	<p>Injection point: (<%= @some_html %>)</p>

<%= button_to "delete", :controller => :static_pages, 
                           :action => 'home',
                           :method => :get %>

<%= form_for like=true, :html => { :method => :delete },
                     :remote => true do |f| %>
    <%= f.submit "Unlike" %>
  <% end %>

  </body>
</html>
